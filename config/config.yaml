app:
  name: ecommerce-pipeline

paths:
  raw: data/raw
  bronze: data/bronze
  silver: data/silver
  gold: data/gold

spark:
  app_name: ecommerce-batch-job
  shuffle_partitions: 200
  broadcast_timeout: 300
  adaptive_execution_enabled: true